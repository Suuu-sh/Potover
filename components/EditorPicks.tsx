import Link from 'next/link';
import ArticleLink from '@/components/ArticleLink';import Image from 'next/image';import {ArrowRight,Clock3,ExternalLink} from 'lucide-react';import {articles} from '@/lib/data';
const logo:Record<string,string>={'Upswing Poker':'/sources/upswing.png','GTO Wizard':'/sources/gto-wizard.png','PokerNews':'/sources/pokernews.png','PokerCoaching.com':'/sources/pokercoaching.png'};const titles:Record<string,string>={'river-bluff-catching-explained':'リバーでのブラフキャッチを科学する','poker-position-guide':'ポジション別レンジ構築の基本','understanding-icm':'ICMを使った終盤の意思決定ガイド'};
export function EditorPicks(){return <section className="editor-picks"><div className="picks-head"><h2>編集部のおすすめ</h2><Link href="/docs">すべて見る <ArrowRight size={15}/></Link></div><div className="pick-list">{articles.slice(0,3).map(a=><article className="pick-row" key={a.slug}><span className={`pick-logo pick-logo-${a.sourceSlug}`}><Image className={a.imageUrl?'pick-article-image':''} src={a.imageUrl||logo[a.source]||'/icon.png'} alt="" width={160} height={96}/></span><div className="pick-copy"><ArticleLink slug={a.slug}><h3>{titles[a.slug]||a.title}</h3></ArticleLink><p>{a.summary}</p></div><span className="pick-source">{a.source}</span><span className="pick-level">{a.difficulty}</span><span className="pick-language">{a.language}</span><span className="pick-time"><Clock3 size={14}/>{a.minutes}分</span><a href={a.url} target="_blank" rel="noreferrer" aria-label="元記事を開く"><ExternalLink size={17}/></a></article>)}</div></section>}
