 import {articles} from '@/lib/data'; import {notFound} from 'next/navigation'; import {LearningLink} from '@/components/LearningLink';
export function generateStaticParams(){return articles.map(({slug})=>({slug}));}
export default function Article({params}:{params:{slug:string}}){const a=articles.find(x=>x.slug===params.slug);if(!a)return notFound();return <main className="shell page shared-header-page"><article className="detail"><div className="source">{a.source}</div><h1>{a.title}</h1><div className="meta"><span className="pill">{a.difficulty}</span><span className="pill gray">{a.language}</span><span>{a.publishedAt} · {a.minutes} min read</span></div><div className="detailbox"><h2>Potover Summary</h2><p>{a.summary}</p><h2>この記事で学べること</h2><p>このページでは記事本文を転載せず、学習テーマと概要だけを紹介しています。詳しい解説は元記事で確認してください。</p><div style={{display:'flex',gap:8,flexWrap:'wrap',margin:'20px 0'}}>{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div><LearningLink className="cta" slug={a.slug} href={a.url}>元記事を読む ↗</LearningLink></div></article></main>}
