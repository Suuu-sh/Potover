 import Link from 'next/link'; import {articles} from '@/lib/data';
export default function Articles(){const list=articles;return <main className="shell page shared-header-page"><div className="pageintro"><h1>記事一覧</h1><p>{list.length}件の記事</p></div><div className="explore"><aside className="filters"><h3>記事を絞り込む</h3><p className="summary">詳細な検索は <Link href="/docs">記事を探す</Link> から利用できます。</p></aside><div className="grid">{list.map(a=><Link className="card" href={`/articles/${a.slug}`} key={a.slug}><div className="source">{a.source}</div><h3>{a.title}</h3><p className="summary">{a.summary}</p><div className="meta">{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}<span>{a.difficulty} · {a.language} · {a.minutes} min</span></div></Link>)}</div></div></main>}
