import {SiteHeader} from '@/components/SiteHeader'; import Link from 'next/link'; import {articles} from '@/lib/data';
export default function Articles(){const list=articles;return <main className="shell page shared-header-page"><SiteHeader/><div className="pageintro"><div className="eyebrow">Library</div><h1>Explore articles</h1><p>{list.length} articles indexed · metadata and summaries only</p></div><div className="explore"><aside className="filters"><h3>Filter articles</h3><p className="summary">詳細な検索は <Link href="/docs">記事を探す</Link> から利用できます。</p></aside><div className="grid">{list.map(a=><Link className="card" href={`/articles/${a.slug}`} key={a.slug}><div className="source">{a.source}</div><h3>{a.title}</h3><p className="summary">{a.summary}</p><div className="meta">{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}<span>{a.difficulty} · {a.language} · {a.minutes} min</span></div></Link>)}</div></div></main>}
