 import Link from 'next/link'; import {articles} from '@/lib/data';
export default function Articles(){const list=articles;return <main className="shell page shared-header-page articles-clean"><div className="articles-clean-toolbar"><strong>{list.length}</strong>件のコンテンツ</div><div className="grid">{list.map(a=><Link className="card" href={`/articles/${a.slug}`} key={a.slug}><div className="source">{a.source} · {a.contentType==='video'?'動画':'記事'}</div><h3>{a.title}</h3><p className="summary">{a.summary}</p><div className="meta">{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}<span>{a.difficulty} · {a.language} · {a.minutes} min</span></div></Link>)}</div></main>}
