import {MetadataRoute} from 'next'; import {articles} from '@/lib/data'; export default function sitemap():MetadataRoute.Sitemap{const base='https://potover.com';return [{url:base},{url:`${base}/articles`},...articles.map(a=>({url:`${base}/articles/${a.slug}`}))]}
