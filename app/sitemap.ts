import {MetadataRoute} from 'next'; import {articles,sources} from '@/lib/data'; export default function sitemap():MetadataRoute.Sitemap{const base='https://potover.com';return [{url:base},{url:`${base}/articles`},{url:`${base}/sources`},...articles.map(a=>({url:`${base}/articles/${a.slug}`})),...sources.map(s=>({url:`${base}/sources/${s.slug}`}))]}
