import {AccountSettings} from '@/components/AccountSettings';
export const metadata={title:'Account — Potover'};
export default function Profile(){return <main className="account-page"><AccountSettings/></main>}
